services:
  - type: web
    name: email-classifier
    runtime: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "uvicorn src.app:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: CARGO_HOME
        value: ./.cargo
      - key: RUSTUP_HOME
        value: ./.rustup