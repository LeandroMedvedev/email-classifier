services:
  - type: web
    name: email-classifier
    env: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "uvicorn src.app:app --host 0.0.0.0 --port $PORT"
    pythonVersion: 3.11.9   # usa versão estável com wheel do tokenizers
